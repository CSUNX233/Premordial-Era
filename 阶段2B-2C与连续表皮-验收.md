# 阶段 2B / 2C 与连续表皮：初步验收记录

记录日期：2026-09-09。范围到阶段 2C 为止；未进入阶段 4/5。

## 已接入的机制

- 身体形态走同一条 `Genome → DevelopingBody → BodyGeometry → OrganicMesh` 链。物质与密度决定三维体积，连续基因决定长宽比、截面、渐细、曲率与圆润度。六形态样本是人工基因覆盖，只用于验证表达范围，不写回世界或冒充自然演化。
- 连续表皮由真实父子连接白名单融合。网格会检查边界边、非流形边、连通分量与有符号体积；近邻但未连接的两根结构保持两个分量。
- 每个身体区域有真实姿态、局部库存和局部做功。循环控制器改变局部目标长度；连接弹簧和角约束把力传到相邻区域；截面随长度变化补偿体积。主动做功受该区域能量限制。
- 水中运动使用包含平移/转动耦合的对称 3×3 阻力平衡；陆地运动要求真实地面接触并承受摩擦反力。原先的 `PropulsionVector` 直接速度加成已经移除。单球、零激活、互易周期和无地面支撑均不能产生可见自推进；多区域非互易形变和有支撑接触可产生有成本位移。
- 世界交换面读取动态姿态。断边会改变传力和输运，封闭面不会交换；原有水深、氧、失水、有限光/矿物、渐长、亲代付费、夭折与衰老机制保留。

## 自动检查实测

在项目根目录执行：

```powershell
dotnet build .\src\NativeEpoch.Headless\NativeEpoch.Headless.csproj -c Release
dotnet run --project .\src\NativeEpoch.Headless\NativeEpoch.Headless.csproj -c Release --no-build -- --verify --steps 600 --ancestors 4
dotnet build .\NativeEpoch.csproj -c Debug
```

2026-09-09 最新实测三个构建均为 0 警告、0 错误，`VERIFY PASS`。固定种子两次指纹均为 `74074C0DECB5BF59`，物质误差 `0`，氧误差 `8.004E-11`。2B 诊断关键值：单球位移 `0`、零激活 `4.441E-15`、互易周期 `3.913E-6`、多区域非互易位移 `4.304E-4`、有地面支撑 `4.287E-4`、无支撑 `0`、弱连接姿态差 `6.403E-3`、局部做功 `7.220E-4`、力平衡残差 `1.301E-18`。

使用 Godot .NET 4.7.2：

```powershell
$godot = 'C:\Users\34229\Desktop\Godot-DotNet\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64_console.exe'
& $godot --headless --path 'D:\godot\原生纪' scenes/MorphologyLab.tscn -- --morphology-smoke
& $godot --headless --path 'D:\godot\原生纪' -- --stage3-smoke
```

形态检查 `PASS`：六个低 LOD 网格均为 `0` 边界边、`0` 非流形边、`1` 连通分量；三分枝为单一连续表面；未连接近邻反例为 `2` 个分量。低 LOD 三角形数为 `2928, 2142, 708, 1172, 1556, 1578`。世界 smoke `PASS`：300 个体、600 身体区域、582 渲染实例，真实移动有能耗，水深合法，物质误差 `2.274E-12`，氧误差 `-4.002E-11`。

## 2C 三种子结果

原始结果：[阶段2C-三种子实验-结果.txt](阶段2C-三种子实验-结果.txt)。三个预定种子中只有 `20261011` 找到一个存活成熟的自然突变后代：`ID 25 / Parent 2 / 谱系 2>25 / generation 1`。共同环境、交换位置和回退对照中，祖先、候选与回退体的繁殖结果相同，因此：候选 `1/3`，变异支持 `0/3`，陆生适应 `0/3`。

补充结果：[阶段2C-有限资源共同培养补充结果.txt](阶段2C-有限资源共同培养补充结果.txt)。每个创始点统一施加一次 `+4` 矿物、半径 `8` 的有限外部脉冲，并进入外部物质账本。水中三组都得到 1 个成熟子代并到达第 2 代，但没有成熟孙代；陆地三组都没有成熟子/孙代。三组仍同分，不能支持候选优势或陆生适应。

结论是实验链已经实际运行并保留亲子追踪、成熟门槛、共同/交叉环境、冻结重放和回退对照；本轮自然样本没有观察到陆生适应。补充对照说明有限资源条件下可以产生真实成熟后代，但不能替代原始实验，也不构成适应阳性证据。

上述 2C 报告来自本轮运动量纲和渲染性能修正之前的模型版本，原始结果保留作为第一轮记录；本次性能修复没有重跑三种子实验，因此不能把旧结果冒充当前模型的复验。

## 性能取舍与实测边界

- 选中个体始终保留连续表皮；最近的至多 8 个个体使用连续表皮与骨架姿态，其他个体使用 MultiMesh 区域代理。骨骼显式使用 rest-transform Skin，当前局部位置/角度与骨段长宽厚缩放驱动画面；细环选择标记不再遮住主体。
- 连续网格缓存最多 64 项；纯 CPU 的场采样、三角提取、法线、骨骼归属和拓扑分析放入最多 4 项的后台队列，主线程每帧最多提交 1 个已经完成的网格。等待期间保留 MultiMesh 代理，转离视野的已完成任务会释放队列槽。
- 皮肤使用完整基因形态拓扑作为静态模板，未发育区域缩放为零，生长和姿态只更新骨骼；控制网络等不改变形态的突变复用同一几何键。近景姿态在相邻 10 Hz 真实快照之间逐帧插值。
- 相机视锥使用带滞回的保护边界；选中和近景保留连续表皮，远景使用按真实区域长轴定向的低模代理，视野外不提交视觉实例。该裁剪不改变任何个体的模拟步。
- 低 LOD 降低采样分辨率；细长和分枝形态保留较高的最低采样数。这个取舍降低远景轮廓与表面精度，不改变基因、物质、运动或繁殖结算。无法把主观视觉损失精确量化为 10%。
- 本机 `NVIDIA GeForce RTX 5070 Ti Laptop GPU`、Godot .NET Debug/F5 配置实测：300 祖先从 8 秒预演开始连续运行 30 秒，末人口 `583`，帧时平均 `3.417 ms`、P95 `3.982 ms`、最大 `26.456 ms`，模拟 `300` 步，实际倍率 `1.000×`，运行错误 `0`。默认 24 祖先连续 10 秒，帧时平均 `3.334 ms`、P95 `3.768 ms`、最大 `13.428 ms`，实际倍率 `1.000×`，运行错误 `0`；窗口内首次未知皮肤完成 1 次后台构建与主线程提交。
- 默认 24 场景在镜头朝向群体时提交 `9` 个可见个体，镜头背向后只保留选中体 `1` 个。300 场景的稳定模拟步平均 `8.297 ms`、展示快照 `1.215 ms`、渲染提交 `1.156 ms`；这些是本机短窗口数据，不代表其他硬件或长期稳态保证。
- 成熟且身体质量不变的个体在末 10 秒内，局部角度峰峰值 `0.002856 rad`（约 `0.164°`），位移 `0.11425` 世界单位。运动来自局部做功和介质反力，但当前祖先的自然动作幅度很小，不能宣称肉眼明显摆动。
- 低 LOD 网格体积与基因预算的样本误差约 0–24%；模拟体积仍使用精确解析预算，显示误差不会改动物质账本。

## 人工验收入口

1. 保存并关闭旧标准版 Godot 窗口，通过桌面“原生纪 - Godot NET”打开项目，按 F5。点击“观察负载 300”，选择生物并滚轮拉近；检查其发育尺寸、动态姿态、深度、局部氧/能量以及控制输出。
2. 按 F2 打开形态样本台。数字 1–6 选择六种人工基因样本，右键拖动旋转，滚轮近看，W 切线框，L 切高/低 LOD；重点看弯曲丝与三分枝是否连续、未呈葡萄串。
3. 暂停后单步观察区域姿态和能耗；在水线两侧比较浸没、含水、摄氧和运动。当前截图：[六形态样本台](artifacts/phase2-morphology-lab.png)、[选中个体及检查器](artifacts/phase2-selected-organism.png)。

## 已知限制

- 水动力是局部各向异性阻力近似，没有长程流体耦合、涡流或真实可变形连续介质；连接位置投影与地面接触也是稳定有界近似。
- 本轮三种子有限实验没有成熟孙代证据，也没有陆生适应阳性；需要更长且预注册的实验才能讨论跨代适应趋势。
- 尚未做长时间承载量复测、目标硬件矩阵或完整 GPU profile；短窗口仅证明本机当前场景维持 1×。
- 自动截图已确认选中水下个体的连续表皮可见；初始个体共享共同祖先基因，外观相似是当前投放规则的真实结果。自然形变只有约 `0.164°`，交互旋转、微小运动与长期缓存仍需用户在窗口中最终目检。
