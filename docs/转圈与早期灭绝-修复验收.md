# 转圈与早期灭绝修复（2026-09-09）

用户反馈：默认 24 生物、未调整环境，原地转圈，约第八代灭绝。本轮只处理现有运动与生命循环，不增加视觉、肺或腿，不自动 Git；完成初验后停下等用户检查。

## 定位

- 上轮的短时位移检查不足以排除绕圈。本轮改看 60–120 秒的净位移、路径长度、活动跨度、访问资源格和转向符号。
- 原好奇心以持续正弦侧向信号驱动，且不同区域朝向的活性表面容易产生持续偏航。更新方向时若把固定世界目标不断拉向当前身体朝向，也会让航向反馈失效。
- 表面切向流与身体旋转的反作用方向相反，左右转向要直接验算，不能通过减小转向量掩盖符号错误。
- 局部代谢原来在高能量时仍固定消耗底物；产能到上限后被丢弃，但底物、氧气仍消耗。这会浪费幼体生长和亲代繁殖材料。
- 食物充足判据原来用身体物质的 35%，可能小于繁殖所需 0.65；新判据包含未完成生长及一次繁殖储备。候选位置的氧气信号现在按候选处水陆介质判断。

未发现“第八代死亡”的硬编码。保留旧版本程序集进行独立基线运行：种子 20260908、24 生物、无干预，1000 秒时仅 7 存活、最高第 7 代，5 个成熟体均缺繁殖材料；3000 秒恢复到 134 存活、最高第 20 代。因此本次未精确复现用户那一次全灭，不将其描述为已经定位的固定代数上限。

## 改动与边界

探索保留世界空间方向，以实际方向误差反馈转向；只有明确侧向线索、危险或停滞才调整方向。运动继续走局部暴露、材料、能量付款和介质阻力求解。

代谢改为按局部能量需求调节，并在容量不足时同步缩放底物、氧气和产能；高能量时保留底物用于生长、储备和繁殖。没有增加外部资源、延长寿命或关闭死亡，也没有强制保存某条谱系。

增加 `--diagnose-ecology`：记录最高实际代数、成熟后代数、每段出生/死亡、成熟个体繁殖材料/能量不足及物质误差。这个诊断会在真实灭绝时停止，不补种。

## 最终版本初验

**实际探索**：默认 24 生物世界预热 30 秒，选健康成熟体再跟踪 120 秒：净位移 39.562、路径 43.069，净位移/路径 0.919，范围 38.40 × 11.63，访问 14 个资源格。不能再仅用“有速度”判断通过。

受控成体 120 秒：净位移 60.669、路径 60.670，访问 18 格。低能量 10 秒：位移 0、平均活动强度 0。固定左右指令 10 秒的航向变化为 -1.827 / +1.676 / +5.182 rad（命令 -0.5 / 0 / +0.5），确认两向纠偏权限。原零指令固有偏航约 +0.168 rad/s，原最大反向修正仅约 -0.021 rad/s，故此前即使反向控制饱和也只能继续转圈。新控制通过有能量成本的局部旋转流抵消偏航，不直接覆盖身体朝向。

**生态**：最终完整运动版本、默认 24 生物、无环境修改、人口安全上限 5000。两条测试均在达到 1200 秒检查窗口后由主 agent 主动结束，未将其后的时间计作通过。

| 种子 | 已出现最高代数 | 1200 秒存活 | 最后 600 秒出生/死亡 | 累计成熟后代 | 物质误差 |
|---|---:|---:|---:|---:|---:|
| 20260908 | 11 | 1102 | 2443 / 2146 | 2994 | -1.364e-12 |
| 20260909 | 12 | 1245 | 2699 / 2155 | 3074 | -6.821e-12 |

两组都越过第八代并保持繁殖，未触及人口安全上限。有限窗口不保证任意种子永不灭绝，也尚未验证长期稳定 K 值。

**基本规则**：主 agent 运行 Release VERIFY PASS，重复指纹 `25F66AB9131A7414`；物质误差 `2.728e-12`，氧误差 `1.310e-10`，按能量需求代谢的新负对照通过，零激活/零能量/水陆限制/局部传输检查通过。Godot Debug 构建 0 警告、0 错误。

可复现入口：

```text
dotnet run --project src/NativeEpoch.Headless -c Release -- --diagnose-movement
dotnet run --project src/NativeEpoch.Headless -c Release -- --diagnose-ecology --seed 20260908 --steps 12000 --ancestors 24 --report-every 6000 --max-population 5000
```

**Godot 压力检查**：唯一一次 1000 祖先、预演 8 秒后测量 30 秒，最终人口 1978；实际倍率 0.997×、错误 0、退出码 0。模拟步均值 38.034 ms，快照 3.326 ms，渲染提交 2.784 ms；帧均值 19.800 ms、P95 84.531 ms、最大 147.892 ms。选中成熟体末 10 秒位移 2.169，身体物质不变；背向视野只保留选中体，稳态新网格构建 1 次。

该负载高于上述正常长跑窗口中的约 1100–1200 活体，1×时间倍率基本保持，但约 2000 活体仍有明显帧尾卡顿，**不宣称流畅 60 FPS 或性能已经全部解决**。性能数字由开发 agent 实测，主 agent 未重复该压力检查。

主 agent 另运行新 CLI `--diagnose-movement`：MOVEMENT PASS；检查要求健康和低能个体都活满各自观测窗口，避免把提前死亡误判为休息。资源格尺度与环境采样间距一致。

本轮初验结束，停止等用户在 Godot 亲自观察。先结束旧游戏进程，再 F5 新开默认 24 生物世界；检查两分钟内是否持续离开原来的区域，以及加速长跑后的实际出生/死亡/代数。未提交 Git，未启动新器官开发。

## 球面版本转圈与捕食核查（2026-09-10）

用户要求运动必须由生物自身产生，不能强制位移。本轮没有增加导航、传送、保底位移或自动脱困。修复两处力学近似错误：区域阻力不再只作用于中心点，还计入有限长度/宽度对应的局部旋转阻力及形变角速度；原最大移动速度限制现同时缩放解出的角速度，避免只限制前进而留下高速转圈。没有修改基因、感知或控制输出。

24祖先、种子20260908、禁止繁殖的观察对照（先30秒成长，再记录120秒）中，多数个体本来就有实际位移，少数运动能力不足。代表个体8累计绝对转角从2.80圈降为1.84圈，净位移25.54→30.08m；个体9累计转角0.18→0.03圈，净位移61.66→64.57m。仍允许无效形态、缺乏合适驱动或环境不适者停滞、转圈；不保证所有个体高效游动。上述累计转角不代表全部绕完整圆周。

MovementBehaviorDiagnostics的路径与净距已改用球面测地距离。MOVEMENT PASS：健康样本120秒净距65.61m、路径65.62m、19个资源网格，低能对照10秒位移和活动均0。600步VERIFY（含零能量/零激活、形变推进、力矩平衡、守恒）通过，Debug/Release构建无警告错误。CLI新增--diagnose-population-movement，输出所有初始个体轨迹而非只测一个优秀样本。

捕食已在正式SimulationWorld.Step中验证，接触同化0.0001841969、分离同化0，世界物质误差4.18e-11；直接模块同化0.0176865、猎物损失0.0378808、返还残骸0.0201942、物质误差0。摄食与消化表达、食欲、能量、容量与防御均限制实际摄取。每个体每步至多尝试最强接触对象，沿用局部宽相，未引入全种群两两搜索。

当前是接触机会性捕食：尚无猎物专用感知、追逐与逃跑决策；直接摄取猎物有机储备并造成少量损伤，不直接撕取活体结构。死亡结构经原尸体回收路径进入环境。不能称为完整主动狩猎生态。FOOD WEB PASS包含正式接触/分离对照及前一轮饥饿修复对照。