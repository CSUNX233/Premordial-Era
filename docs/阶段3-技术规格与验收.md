# 阶段 3：低模世界表现与验收修复

日期：2026-09-08。状态：用户首轮人工验收提出的五项问题已实现修复，形态样本台检查点 A 已完成自动验证；等待用户在 Godot .NET 编辑器中再次亲自验收。阶段 2 仍按用户要求整体跳过，仅加入本次明确授权的有限基础运动与空间分离。阶段 4、5 未授权。

## 已实现规格

- 根项目 `NativeEpoch.csproj` 使用 `Godot.NET.Sdk/4.7.2` 和 `net9.0`，引用独立的 `NativeEpoch.Simulation`；模拟核心不引用 Godot。`project.godot` 已声明 `C#` feature、程序集名和 F5 主场景。
- 地形从连续环境场采样，使用 `4 × 4` 个低模区块，总计 `48 × 48` 个采样格；水面为共享平面。自然、高度、温度、光照、矿物和残骸图层使用顶点色切换。
- 世界里的身体区域暂用共享低模球面经连续长宽厚缩放为椭球，并由一个 MultiMesh 批量显示。显示比例已从原先的 `2.4` 倍改为与当前模拟几何同尺度；超过 `20,000` 个区域时，未选中个体使用共享代理，选中个体始终保留完整区域，不删模拟实体。
- W/S 已按相机水平视线计算：W 向画面所看的前方，S 后退；A/D 横移。右键按住拖动旋转，滚轮缩放下限降至 `2.2`，相机近平面为 `0.03`；选中个体时自动把目标移到该个体并允许近看。点选采用屏幕投影，地形笔刷射线迭代贴合实际高度。
- 右键只控制相机。矿物/温度笔刷使用左键增加、`Shift+左键` 减少，连续半径平滑衰减。矿物变化进入显式外部物质账本。
- F11 切换全屏。UI 用 CanvasLayer、Control、Container 和共享 Theme；顶栏、工具栏、检查器分离。窗口宽度跨过 `940` 与 `620` 像素时自动折叠侧栏，样本台窄窗口把详情面板移到底部。统计约 2 Hz、选中详情约 5 Hz 且值不变时不重写。
- 暂停、单步和 `1×/10×/100×/1000×` 始终执行同一个 `0.1` 秒固定步；倍率只改变单位现实时间内执行的固定步数量。

## 有限基础运动

本次运动是对“生物完全不动且拥挤重叠”的验收修复，并非补完阶段 2：

- 身体材料的连续收缩性、刚性、信号连接、区域角度、质量和阻力共同导出宏观推进向量；个体朝向把它变换到世界坐标。
- 位移真实修改模拟坐标。每段实际位移按身体激活成本扣除能量，并累计到 `CumulativeMovementEnergy`；能量不足时同比缩短位移，零能量不能免费推进。
- 局部空间哈希只检查相邻桶，有限软分离对成对个体对称施力；完全重叠时按 ID 确定性选方向。边界只钳制并衰减反射速度，不删除或随机传送个体。
- 新生个体从亲代附近出生并继承带小幅偏差的朝向，随后由同一真实运动与分离规则离开拥挤区。

这是确定性的有限自推进近似，没有感知、目标追踪、控制网络、捕食或生态位。它也不是低雷诺数流体求解，不能把均匀球体的往复收缩称作自然微生物游泳。

## 形态样本台 A

`res://scenes/MorphologyLab.tscn` 是独立诊断场景，可从世界工具栏或 F2 打开。它不创建、替换或修改模拟个体。

- 一个统一生成器接收连续骨架段、起止半径、厚度比例、渐细和连接混合参数，展示人工构造的球形、卵圆、渐细杆、弯曲丝、扁平体和三分枝六种样本。
- 生成器对受连接骨架约束的隐式场做 marching tetrahedra 等值面提取。三分枝的主干与三个分枝共享连接点和局部平滑混合，输出一个连续曲面，不是独立球体或椭球的葡萄串。
- 左键或数字 1–6 选择样本；右键拖动旋转、滚轮近看；W 切换线框，L 切换高/低 LOD；面板显示参数、连接段数、采样分辨率和实际三角形数。
- 高 LOD 六个样本的实际三角形数为 `2832, 1825, 820, 836, 1112, 1102`。这些是样本台当前实测值，不是世界群落性能承诺。

样本台明确标注“人工几何测试输入，非自然演化结果”。当前正式世界仍用阶段 1 区域数据映射到过渡椭球；新曲面尚未进入遗传、物质体积、碰撞代理或摄取表面积，检查点 B 未授权且未实施。

## 工程入口

- 项目：`D:\godot\原生纪\project.godot`
- .NET 编辑器：`C:\Users\34229\Desktop\Godot-DotNet\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64.exe`
- 桌面入口：`原生纪 - Godot NET.lnk`
- F5 主场景：`res://scenes/Stage3Main.tscn`

原标准版 `Godot_v4.7.2-stable_win64.exe` 不含 C# 支持，不能运行本工程的 C# 场景。

## 人工验收步骤

若旧标准版窗口仍有未保存内容，先保存真正需要的场景并关闭旧窗口，不要用旧设置覆盖当前 `project.godot`。随后从桌面“原生纪 - Godot NET”打开项目并按 F5。

1. 先确认 W 向当前镜头所看的水平前方移动、S 后退；在世界空白处长按右键拖动，确认镜头持续旋转。右键不再减少笔刷。
2. 选择工具点击一个生命。镜头应聚焦它，继续向上滚轮可拉近到具体身体；近距离不应被旧的 35 单位下限挡住，也不应被近平面立即切穿。
3. 拖动窗口缩小、放大，并按 F11 往返全屏。顶栏控件可换行；中等宽度隐藏检查器，窄窗口进一步隐藏工具栏，可用顶栏按钮按需展开，不遮满世界。
4. 点击“观察负载 300（预演 8 秒）”，用 1× 或 10× 观察位置变化和邻近个体逐渐分开。检查器应显示速度和由身体缓存导出的推进向量；统计显示均速。暂停后模拟坐标停止变化。
5. 选择矿物或温度笔刷：左键增加，`Shift+左键` 减少。切换热力图观察连续衰减范围，并确认矿物干预后的物质误差仍接近零。
6. 按 F2 打开形态样本台。近看六个样本，尤其检查三分枝连接处没有球体穿插缝；按 W 看线框、按 L 比较 LOD，选择后核对参数和三角形数。按 F2 或 Esc 返回世界。

## 自动验证记录

```powershell
dotnet build .\NativeEpoch.csproj -c Release
dotnet run --project .\src\NativeEpoch.Headless\NativeEpoch.Headless.csproj -c Release -- --verify
& 'C:\Users\34229\Desktop\Godot-DotNet\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64_console.exe' --headless --path 'D:\godot\原生纪' -- --stage3-smoke
& 'C:\Users\34229\Desktop\Godot-DotNet\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64_console.exe' --headless --path 'D:\godot\原生纪' res://scenes/MorphologyLab.tscn -- --morphology-smoke
```

截至 2026-09-08 的真实结果：

- Release 构建：`0` 警告、`0` 错误。
- 阶段 1/模拟回归：`VERIFY PASS`；固定种子两次指纹均为 `AB6FEDCBFD336200`，人口 `1361`，出生 `1389`，死亡 `32`，平均速度 `0.542036`，累计移动耗能 `1358.274022`，缓存误差 `0`，物质误差 `-2.910383E-11`。
- 世界冒烟：`STAGE3_SMOKE PASS`；步 `81`、人口 `600`、身体区域/渲染实例 `1500/1500`、两次干预、W 前进/S 后退、右键拖动、近距缩放与三档响应式侧栏路径均通过，移动已耗能，平均速度 `0.7072`，物质误差 `-2.910383E-11`。
- 样本台冒烟：`MORPHOLOGY_SMOKE PASS`；高 LOD 为 `2832,1825,820,836,1112,1102`，低 LOD 为 `1224,824,312,368,312,452`，三分枝为单一曲面，线框切换有效，所有低 LOD 三角形数均下降。
- Godot .NET 4.7.2 headless 编辑器已完成文件扫描和 C# 类注册，两个场景均无脚本实例化错误。

## 尚未验证与阶段门

- 自动冒烟能验证命令、数据和场景实例化，不能替代真实鼠标拖动、视觉连接质量、缩放手感和窗口布局的人眼验收。
- 尚未在 RTX 3060 测平均 FPS、1% Low、CPU/GPU 时间、内存或显存，不能声称达到目标性能。
- 形态样本台是检查点 A。检查点 B 的遗传/物理几何统一和检查点 C 的群落 LOD/缓存优化均未授权、未实施。
- 本轮完成后停止，等待人工验收；不得自动进入阶段 4 或 5。


## 窗口尺寸修复（2026-09-08，主 agent）

确认本机游戏嵌入配置未指定尺寸策略，Godot 默认固定尺寸。已在本项目编辑器元数据中关闭嵌入式运行，并把可选嵌入尺寸策略设为 Stretch to Fit；项目显式允许调整窗口尺寸，移除初始窗口 override，保留 canvas_items/expand 缩放。

桌面新增“原生纪 - 直接运行”，直接启动同一 .NET 游戏主场景，可立即使用且不依赖正在运行的编辑器缓存。若继续通过 F5，先重启编辑器使其重新读取本项目设置；若旧编辑器退出覆盖设置，可在 Game 工具栏关闭 Embed Game on Next Play，或将 Embedded Window Sizing 改为 Stretch to Fit。游戏 F11 全屏应在独立窗口验收。

主 agent 已使用真实 D3D12 图形窗口运行主场景进行尺寸验证：960×540时缩放0.75，1600×900时缩放1.25，900×700时逻辑视口扩展为1280×995，Fullscreen实际2560×1600、逻辑1280×800，输出 WINDOW_RESIZE PASS。并非只在无画面模式中检查宽度参数。
