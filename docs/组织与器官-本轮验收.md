# 组织、器官与图鉴：本轮验收

日期：2026-09-09。代码已接入正式 Godot 世界；完成后等待用户亲自检查。

## 交付内容

- 五类连续组织表达，发育投入与维护成本；最多 8 个可遗传受体槽，受体来源、通道、方向和控制连接可变异。弱、无效或有害组合不被自动修正成有利器官。
- 简单方向光感：局部光线与对比度、有界地形射线检查、身体自遮挡、能量和连接限制。不是图像识别，也没有识别其他生物外观的视觉系统。
- 三维附肢：真实区域链、可遗传休止俯仰和活动度，局部付费驱动，最多 8 个足端地形接触。被动结构可支撑；无能、悬空、断链不推进。三维姿态直接用于近景皮肤和区域显示。
- 气腔：有限腔氧、固定位置开口、实际空气接触、通气能耗、交换与运输、进水和死亡归还。开口被遮住不能自动移动到别的外露位置，埋入地形不能当作空气。
- 功能图鉴：按 G 查看、收藏、筛选和定位实际观察到的作用与组合，持久化首次代数和基因指纹。收藏不会改写基因。

## 核心短测

| 检查 | 结果 |
|---|---|
| Release / Godot Debug 构建 | 0 警告、0 错误 |
| 确定性与生命循环 `--verify` | PASS，指纹 `4CF54E3ABBA3135C`；60 秒 4 祖先产生 3 次出生 |
| 表达 / 感知 | 无表达、无能量、未发育来源信号为 0；付费表达与遗传检查通过 |
| 简单视觉 | 朝向信号约 0.04356，背向约 0.00568；地形/身体遮挡、无表达、无能为 0 |
| 三维附肢 | 付费位移 0.038616；零能、悬空、断链为 0；被动支撑为 1；自身平移反馈误差 6.66e-8 |
| 气腔 | 空气补给约 0.035239；封闭补给 0；死亡归还；最大氧账本误差 1.42e-14 |
| 运动 / 竞争 | PASS；120 秒运动净距 30.659，低能活动为 0；局部分配与出生无损检查通过 |
| Godot 交互 | 相机、近距缩放、响应布局、介质深度与守恒通过 |
| 图鉴 | JSON 往返、收藏二次覆盖保存与重载、跨世界 ID 防误选、暂停恢复通过 |
| 后台模拟 | 与同步同种子、同固定步数的完整状态指纹一致；暂停、笔刷串行化和重置通过 |
| 最终默认种子短程生态 | 24 祖先，120 秒：55 活体、32 出生、1 夭折、17 基因组；物质误差 3.64e-12，氧误差 3.27e-11 |

## 性能与精度

本轮没有通过降低模拟步长精度或跳过不可见生物来换取速度。使用区域样本复用、无捕获查找、固定栈缓冲、求解器容器复用和未表达关节的快速路径。复杂双附肢 20,000 步约 123.92 ms，线程分配总计 6,232 B；这是独立模块微测，不是游戏帧率。

Godot 改为一个后台批次独占可变模拟，主线程读取完成后的快照。输入修改、重置和退出先完成批次；相机读取单独的静态地形缓存。每批约 8 ms 预算，单个较重模拟步可能超过预算；固定步仍为 0.1 秒。出现模拟异常会暂停，保留最后展示快照，允许重置。

用户允许的 10% 精度下降没有作为默认折扣使用：缓存优化保持相同公式，后台对照指纹一致；现有 8 受体/8 接触点属于模型容量边界，不能宣称它相对完整真实物理只损失 10%。

最终实际窗口采样：Windows / Godot 4.7.2 .NET / D3D12 Forward+ / RTX 5070 Ti Laptop，1000 祖先模式，约 996 活体，30 秒。目标 1×，测得约 1.006×（测量边界包含完整步）；平均帧 3.348 ms，P95 4.742 ms，最大 23.164 ms，运行错误 0。核心单步 38.323 ms，快照 3.802 ms，渲染提交 4.910 ms。按步骤累计的内存分配仍高，约 33,958 KB/步；该数值是分配总量，不是常驻内存。

收紧最后一项感知成本之前，同一 1000 祖先预设的同步界面对照：P95 13.215 ms、最大 61.385 ms。此对照用于说明界面线程阻塞的影响，不作为严格同版本的精度差证明。数值一致性由最终后台/同步完整状态指纹测试验证。

## 证据边界

这些短测证明已建模的组织组合可以产生作用、付出代价并遗传，不证明完整肺或腿已在默认种群中自然演化成功，也不保证每次运行都会上岸。图鉴记录观察证据，不能把一个新外形或一次存活宣称为新物种或跨代适应优势。

旧版本约 1000 只的长期 K 校准不能直接代表新增组织成本后的平衡。当前仍是近似模型：简单视觉只处理有界光信号，身体接触与资源场并非完整三维连续介质，气腔为区域内混合库存。尚需玩家长程观察评估各谱系的适应和承载变化。
