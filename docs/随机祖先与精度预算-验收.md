# 随机水生祖先、精度预算与稀疏突变

2026-09-09，承接已通过的组织/器官/图鉴版本。

## 初始祖先

正常世界默认 `RandomizeFounders=true`，每个祖先使用独立的确定性随机流生成基因。区域数为 1–4，核心比例、扁度、曲率、分枝连接、材料、表达、代谢、控制权重和受体参数均有差异。共同约束是基础水生：低保水能力，无方向视觉、气腔或可用关节，初始年龄与成熟度为 0。

幼体只从自己的核心开始，其他区域仍需正常发育与投入。核心目标物质至少 0.40，确保默认初始 0.25 物质合法。后续缩小突变的子代，初始核心物质按目标体型限制，亲代扣除同样的实际物质，不扩大基因目标体型或免费增加物质。

`--diagnose-founders`：种子 20260909，24 个祖先有 24 种基因、24 种核心几何、4 档区域数；同种子状态和位置一致，换种子不同；全部处于合法水深；显式统一祖先兼容；不同起始数量的共同预算保持守恒。初始差异属于初始条件，不标记为出生突变或新物种。

## 精度换取计算量

| 配置 | 表面采样/区域 | 姿态功能几何刷新 | 发育派生几何刷新 |
|---|---:|---:|---:|
| Reference | 16 | 每步 | 每步 |
| Balanced（默认） | 14 | 每 2 步 | 每 2 步 |

固定步仍是 0.1 秒；派生几何最多滞后一个模拟步，拓扑新增立即刷新。质量、物质库存和 BodyCache 每步精确更新，环境扣账、能量支出、死亡归还和遗传不跳过。相机可见性不影响精度配置。

`--diagnose-world-precision` 使用随机 300 祖先、种子 20260908、同物质预算，关闭突变以隔离精度影响；暖机 80 步后测完整 300 步，交错执行 3 批取中位数：

- Reference：4059.7362 ms；Balanced：3574.3473 ms，完整 Step 耗时减少 **11.96%**。
- 存活 529 → 526，出生 231 → 229；能量 7387.54 → 7399.12；平均速度 0.180831 → 0.181843；创始者平均位移 5.23570 → 5.23723。
- 主要相对输出偏差最大 **0.8658%**。死亡数 2 → 3，单列为原始队列的绝对差 **1/300=0.3333 个百分点**，不声称死亡数相对误差小于 10%。
- 物质误差不超过 5.46e-12，氧误差不超过 7.64e-11，有限数值检查通过。
- `--diagnose-precision` 的局部摄取、氧、光、水、能量作用量最大相对偏差 **3.8914%**。

这些是指定工作负载下的对照，不保证任意体型、环境和长程谱系分化都保持同一耗时或偏差。这里的 11.96% 是完整模拟 Step 的实测，不是渲染帧时间，也不是把某个模块的节约比例套到整个游戏。

## 突变概率

保留可遗传参数 `p=MutationRate`，新总概率为 `1-(1-p)*(1-0.55p)+0.05`；p 为 0 时总概率仍为 0。初始 p=0.22–0.32，对应 **36.438%–48.968%**，比旧模型高 5 个百分点。

典型 p=0.28：总概率 **44.088%**。触发后抽取 1/2/3 个事件的比例为 90%/9%/1%，所以抽样前理论上无事件/1/2/3 次分别约为 **55.912% / 39.679% / 3.968% / 0.441%**。无效或没有改变最终基因的尝试不算实际突变，因此实测次数可能略低。

按用户后续调整，结构类占事件约 **15%**，局部标量类约 **85%**；结构类包含区域复制、删除、重连及感知/控制连接变化。结构变化不保证有利，局部变化也可能通过连接和表达产生较大功能影响。

这些事件作用于高层形态和功能参数，不是 DNA 碱基。设计参考[不同突变类型的效应分布研究](https://journals.plos.org/plosbiology/article?id=10.1371/journal.pbio.3002698)，借用小变化为主、少数大效应的建模思路；具体游戏概率不是该论文测出的生物常数。

## 最终整合检查

固定亲代 10,000 次抽样：任意突变 43.66%（理论 44.088%）；条件 1/2/3 次为 89.17% / 9.64% / 1.19%；实际结构事件占 14.43%（目标 15%）。3331 次单局部事件均只改变一项标量，183 次 SensorPoint 同样仅改变一项；无虚假突变、非法基因或事件元数据错误，零概率精确继承通过。

Release 构建无警告、无错误。最终种子 20260908、4 祖先、600 步 VERIFY PASS，重复指纹均为 1A392ADABF9FEC70；物质误差 9.09e-13，氧误差 6.55e-11。24 祖先、自然突变启用、1200 步后存活 71，累计出生 48、死亡 1，平均速度 0.246，物质和氧账本通过。

Godot STAGE3_SMOKE 与 WORKER_SMOKE 均通过，包括相机、窗口适配、近距缩放、图鉴保存/暂停，以及后台与同步确定性、暂停、命令串行化、重置。个体检查器显示实际出生突变次数和子代突变概率。此轮按用户要求保存 Git 后等待人工检查。