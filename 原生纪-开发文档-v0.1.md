# 《原生纪》第一版开发文档

> 英文名：待定  
> 文档版本：0.1  
> 项目阶段：概念验证 / 首个可玩版本  
> 核心命题：只设计世界的底层规则，让生命自行给出答案。  
> 已确认方向：连续空间、实验箱玩法、自由干预、可调时间倍率、长期存档。

---

## 1. 文档目的

本文档定义《原生纪》首个可玩版本的产品目标、模拟边界、核心系统、技术架构、性能预算与验收标准。

第一版的任务不是模拟完整现实，也不是保证生命一定演化出智慧，而是验证以下闭环：

1. 世界提供空间、能量与生存压力。
2. 基因经过发育规则生成身体与行为。
3. 生物摄取资源、生长、繁殖与死亡。
4. 后代发生可遗传的微小变化和偶发结构变化。
5. 环境选择使不同谱系兴衰、迁移并逐渐分化。
6. 玩家能够观察并理解这些变化是如何发生的。

只要游戏能稳定地产生数个长期共存、具有不同生存策略的谱系，并让玩家追溯它们的共同祖先，第一版即视为成功。

---

## 2. 产品定义

### 2.1 类型

- 单机人工生命模拟器
- 生态观察沙盒
- 连续空间的低模 2.5D 世界
- 非关卡制、非角色扮演、无传统胜负条件

### 2.2 玩家身份

玩家是世界规则的设定者和自然史观察者，而不是某只生物的直接操控者。

第一版允许玩家：

- 创建具有随机种子的世界。
- 调整初始环境参数。
- 放置一个或数个共同祖先。
- 暂停、单步和以多个倍率运行模拟。
- 查看环境热力图。
- 观察个体、物种和谱系。
- 查看关键演化事件与灭绝记录。
- 使用具有连续半径和衰减范围的工具改变局部地形、气候与资源。

第一版暂不加入资源消耗、科技树、任务系统或“神力点数”。所有干预工具均作为免费的实验工具。

### 2.3 核心体验

玩家应当经历这样的过程：

1. 看到少量简单生命进入空白世界。
2. 发现个体之间存在细微、可遗传的差异。
3. 看到某种结构在特定环境中带来优势。
4. 看到一个谱系扩张、迁移或灭绝。
5. 看到共同祖先逐渐分裂成多个生态策略不同的物种。
6. 能够回溯某种结构或行为第一次出现的时间，并理解系统为何把它识别为某种功能。

### 2.4 设计支柱

#### 涌现，而非剧本

开发者定义物理、生理和遗传规则，不直接规定“第几代出现捕食者”或“某物种必然胜利”。

程序内部不得把眼睛、肌肉、胃、鳍、翅膀、生殖器官等作为可选身体部件写死。开发者只定义材料属性、局部相互作用、能量与物质规律、信号传播和发育指令。器官是多个结构在环境中表现出的结果，不是基因菜单中的预制选项。

#### 可理解，而非纯随机

变化必须可以通过基因、身体、环境和祖先记录得到解释。玩家看到的不应只是随机数字波动。

#### 复杂度决定成本

模拟成本由生物包含多少性质不同的结构区域与连接决定，而不是由它隐含拥有多少真实细胞决定。

#### 历史不可替代

每个世界都有种子、谱系树、灭绝事件和独特命名。玩家观察的是一段不可复制的自然史。

#### 世界连续存在

世界没有关卡回合和固定终局。它从被创建起拥有一条连续时间线，保存后可以从同一时刻继续运行。生物自由移动，环境变化连续过渡，不向玩家暴露方格和离散生态层。

#### 性能是底层规则

首版必须在桌面 RTX 3060 级显卡上以最低必要面数流畅观察标准世界。生态密度和时间加速优先于模型精度、实时阴影和复杂特效。任何新增自然规律都必须同时说明单个个体成本和全世界成本，不能把性能问题留到内容完成后处理。

---

## 3. 第一版范围

### 3.1 必须完成

- 有限但空间连续的二维高度场，并以低模三维方式显示。
- 连续变化的陆地、水深、水下高度、光照、压力和温度。
- 光照、温度、水深、矿物和有机物等环境字段。
- 基因组、发育、身体结构、代谢、行为、繁殖和死亡。
- 无预制器官枚举；身体部位的形态和用途由连续材料属性、结构连接与环境相互作用涌现。
- 无性繁殖、点突变、结构复制、结构删除和连接变异。
- 同一组底层反应规则至少能够自然形成三类营养结果：利用光、从环境吸收物质、分解其他生命或残骸；它们不是可选职业或预设器官。
- 简化的可遗传行为网络。
- 个体谱系、物种聚类、物种命名和系统发育树。
- 从暂停、正常观察到无画面极速演算的时间倍率控制。
- 个体检查器、环境热力图和生态统计。
- 手动存档、自动存档、固定随机种子和基础回放能力。
- 无画面运行的加速模拟模式，用于测试数百至数千代。
- 在 RTX 3060 级桌面显卡的标准测试机上，以低模画质流畅运行标准世界。

### 3.2 明确不做

- 不模拟每一个真实细胞。
- 不模拟原子、分子反应或真实流体力学。
- 不模拟真实三维流体、体素空间或完整刚体飞行；水生生物只额外拥有连续深度轴。
- 不模拟完整器官内部结构、血液循环或真实神经生物学。
- 不承诺第一版自然产生语言、文明或高等智慧。
- 不加入联网、多玩家或云端持续世界。
- 不依赖生成式 AI 决定生物行为。
- 不为每个物种手工制作模型和动画。
- 不在第一版实现有性繁殖、寄生、疾病和复杂共生。

### 3.3 后续保留方向

- 有性繁殖和物种间繁殖隔离。
- 信号、记忆、合作、欺骗与社会关系。
- 寄生、共生、疾病和免疫。
- 空中、地下和洞穴空间。
- 更完整的三维姿态、立体水流与垂直生态系统。
- 世界性灾害、气候周期和大灭绝。
- 玩家策展、世界分享和物种导出。

---

## 4. 世界模型

### 4.1 坐标与表现

世界对生物和玩家表现为连续空间。陆地生物使用浮点二维坐标 `(x, y)` 自由移动，不存在格子中心、格子边界或逐格移动。地形是一个连续高度函数，渲染时映射为三维坐标：

```text
地形高度 = TerrainHeight(x, y)
渲染坐标 = (x, 地形高度, y)
```

水深同样由连续函数得到：

```text
水深(x, y) = max(0, WaterSurface(x, y) - TerrainHeight(x, y))
```

为了性能，温度、矿物等环境字段允许在底层使用规则采样阵列或分区缓存，但所有查询必须通过双线性插值、平滑函数或解析函数返回连续值。采样边界不能直接影响生物移动、资源摄取或玩家工具。

第一版采用边界有限、内部连续的岛屿世界，由深海和不可穿越缓冲区包围。有限只代表世界有范围，不代表内部空间离散。

### 4.2 建议规模

世界范围和环境采样精度分开设置。首版标准世界采用：

- 连续世界尺寸：`512 × 512` 世界单位。
- 普通成年生物的典型长度：`1–4` 世界单位。
- 开发期环境采样：`128 × 128` 个隐藏采样点。
- 第一版目标采样：`256 × 256` 个隐藏采样点。
- 生物坐标不受采样精度限制，可以位于范围内的任意浮点位置。

地形渲染按区块生成，环境采样精度可以独立调节。提高采样精度只让场变化更细腻，不改变世界坐标体系，也不使旧生物的位置失效。

首版只提供这个标准尺寸，优先把生物密度、环境梯度和演化速度调好。后续再加入 `256 × 256` 实验皿和 `1024 × 1024` 大型世界；不以无限地图或超大地图作为首版目标。

### 4.3 环境字段

模拟核心对外提供连续环境采样接口：

```csharp
interface IEnvironmentField
{
    EnvironmentSample Sample(Vector2 position, float depth);
}

struct EnvironmentSample
{
    float TerrainHeight;
    float WaterSurface;
    float WaterDepth;
    float Temperature;
    float Light;
    float Minerals;
    float Detritus;
    float Moisture;
    Vector2 Flow;
}
```

字段内部可以来自噪声函数、曲线、低分辨率缓存或这些方法的组合。生物系统不读取底层采样单元，只读取当前位置的插值结果。

说明：

- `TerrainHeight`：决定山地、平原、浅滩和海沟。
- `WaterSurface`：第一版以局部静态或缓慢变化为主。
- `WaterDepth`：当前位置水面到地形表面的连续距离。
- `Temperature`：受纬度、高度、水体和季节影响。
- `Light`：受昼夜、季节、水深和浑浊度影响。
- `Minerals`：生长所需的基础物质，可缓慢恢复或随侵蚀迁移。
- `Detritus`：尸体和排泄产生的有机残骸。
- `Moisture`：限制陆地生物的生存和繁殖。
- `Flow`：影响漂浮生命、扩散和局部迁移。

### 4.4 水域深度

水域不切成具有硬边界的浅水层、中层和深水层。水生生物保存连续 `Depth` 值，并可以在允许范围内连续上浮或下潜：

```text
0 = 水面
Depth = 当前水下深度
SeabedDepth(x, y) = 当前海底深度
合法范围：0 ≤ Depth ≤ SeabedDepth(x, y)
```

光照、温度、压力和浮力随 `Depth` 连续变化。“浅水”“中层”“深海”“海底”只作为 UI、统计和物种生态位描述，不参与硬性规则判断。

环境主体仍按二维连续场采样，水下属性由二维表面场和深度函数共同计算，不建立昂贵的完整三维体素场。邻居搜索先在平面空间索引中找候选者，再比较连续深度差；双方只有在三维接触距离内才能交互。

### 4.5 环境更新频率

不同系统不必每帧运行：

| 系统 | 建议频率 |
|---|---:|
| 渲染 | 60 FPS 目标 |
| 基础模拟步 | 10 次/秒 |
| 行为决策 | 5 次/秒 |
| 代谢与资源交换 | 2 次/秒 |
| 温度与物质扩散 | 1 次/秒或分区轮询 |
| 分类统计 | 每若干模拟年一次 |

加速时不要求渲染每个模拟步。

---

## 5. 生命表示

### 5.1 基本原则

游戏不持续模拟每个细胞，而使用以下层级：

```text
基因组
  ↓ 编译与发育
身体结构图
  ↓ 汇总
个体宏观属性
```

“细胞”只是一种发育语义。成年生物由少量结构模块构成，每个模块代表大量隐含细胞。

### 5.2 个体数据

```csharp
struct Organism
{
    ulong Id;
    ulong ParentId;
    int GenomeId;
    int SpeciesId;
    Vector2 Position;
    float Depth;
    float Rotation;
    float Age;
    float Energy;
    float StoredMatter;
    float Health;
    float Maturity;
    int BodyStart;
    int BodyCount;
    int SignalStateStart;
}
```

正式实现应优先使用连续数组和整数索引，避免让每个生物成为包含大量引用的独立对象。

### 5.3 身体结构图

身体是一个由无语义区域和连接组成的图。节点只有形状、材料和状态，不携带“肌肉”“眼睛”或“胃”等器官类型：

```text
遗传载体区域 A
├─ 区域 B：细长、柔软、可收缩
├─ 区域 C：薄、透光、对光产生电化学响应
├─ 区域 D：中空、可渗透、能分泌催化物
└─ 区域 E：高密度、高韧性、低渗透
```

如果 B 的排列能够产生推进，它可能被观察系统描述为运动附肢；如果 C 暴露在表面、连接控制网络并改变个体对光的行为，它可能被描述为感光结构。这些名称只存在于 UI 和分析层，模拟核心不知道它们是什么器官。

每个结构模块保存：

- 相对父结构的位置和角度。
- 长度、宽度、截面、表面积和方向性。
- 密度、弹性、刚度、韧性和收缩性。
- 渗透、吸收、分泌、催化和储存属性。
- 对光、温度、压力与化学物质的响应向量。
- 内部信号的产生、传导与衰减属性。
- 由材料属性推导出的质量、维护和激活成本。
- 健康和局部损伤。
- 对称、重复与发育顺序。
- 几何连接、物质连接和信号连接。

第一版将结构数量限制在每只生物 `1–32` 个，以防止无上限复杂化。

每个结构模块代表一片性质相近的组织区域，而不是一个细胞。区域内部细胞数量是隐含的；只有在属性梯度或几何差异超过阈值时才拆分成多个模块。

### 5.4 底层材料属性与作用规律

首版提供的不是器官，而是一组所有生命共同遵守的底层能力：

| 底层属性 | 引擎只定义的作用规律 | 可能涌现的宏观用途 |
|---|---|---|
| 密度、刚度、弹性、韧性 | 决定质量、形变、断裂和力的传递 | 骨架、外壳、软体结构 |
| 主动收缩 | 消耗能量，沿材料方向产生力 | 肌肉、泵、鳍、蠕动组织 |
| 渗透与吸收 | 按表面积、浓度差和材料系数交换物质 | 根、鳃、肠壁、滤食结构 |
| 催化与分泌 | 改变接触物质的分解和转化速率 | 消化腔、毒液、体外消化 |
| 光化学响应与发光 | 暴露表面根据光谱产生能量或信号，也可耗能发出光 | 光合表面、遮光结构、感光点、生物发光 |
| 化学响应 | 对局部物质浓度产生内部信号 | 嗅觉、味觉、趋化结构 |
| 压力与形变响应 | 接触或形变转化为内部信号 | 触觉、平衡、振动感知 |
| 信号传导 | 信号沿连接传播并衰减 | 神经束、反射弧、分布式控制 |
| 储存与运输 | 物质沿连接扩散并可局部积累 | 脂肪、液囊、循环网络 |
| 色素与表面纹理 | 改变吸光、反光、散热和可见信号 | 伪装、警戒色、求偶标记 |

所有属性连续可变。基因不是从表格第三列选择器官，而是在有限材料预算中分配前两列的属性。提高某项属性会增加合成、质量、维护或激活成本，因此不存在所有属性同时拉满的万能材料。

底层结算不得包含类似以下逻辑：

```text
if PartType == Eye: 看见目标
if PartType == Wing: 获得飞行
if PartType == Stomach: 提高消化效率
```

它只能计算局部光响应、力、物质交换、催化反应和信号传播。宏观功能必须由结构位置、形状、连接方式和实际行为共同产生。

### 5.5 派生功能与宏观缓存

身体结构改变后，重新计算并缓存：

- 总质量、质心和转动惯量。
- 外轮廓、暴露表面积、阻力和浮力。
- 结构刚度、受力路径和薄弱连接。
- 收缩区域到身体各处的力映射。
- 渗透、催化和光化学表面的空间分布。
- 物质运输图和内部信号传播图。
- 各材料的静态维护成本与最大激活成本。
- 包围范围与用于邻居粗筛的碰撞边界。

正常模拟步直接使用这些物理缓存，不遍历全部结构。只有生长、受伤或变异导致身体改变时才重新汇总。

“推进能力”“感光能力”“摄食方式”等语义标签由观察系统通过实际测试推断，例如比较个体开关某组结构时的运动、能量交换和行为变化。标签只用于图鉴、分类与解释，不参与适应度计算。

### 5.6 涌现的边界

涌现并不意味着系统可以产生底层规则完全不支持的能力。没有声波传播规则，就不会真正演化出声呐；没有物质分泌和识别规则，就不会形成化学交流。

第一版底层世界至少提供五类开放交互通道：

1. 光与表面响应。
2. 接触、压力、形变和主动收缩。
3. 温度与热交换。
4. 多种环境化学物质及其扩散、吸收、分泌和催化。
5. 生物体内部可遗传的信号产生与传导。

后续增加声音、电场等通道时，应作为所有生命都可利用的新自然规律加入，而不是作为某个预制器官解锁。

### 5.7 可写死与不可写死的边界

允许写死的是所有对象共同遵守的自然规律和计算安全边界：

- 能量、物质、力、扩散、反应和损伤公式。
- 材料属性的成本、冲突与取值范围。
- 基因复制、突变和局部发育指令的语法。
- 遗传载体成为独立生命所需的最低条件。
- 结构数量、控制网络规模和数值稳定性上限。

不可写死的是生命对这些规律的具体利用方式：

- 预制器官、固定物种和身体模板。
- “寻找食物”“攻击敌人”“逃跑”“繁殖”等脚本行为。
- 对复杂度、智慧或某种外形直接给予适应度奖励。
- 因为观察系统把结构称为“眼睛”就让它获得额外视觉能力。

判断原则：删除所有 UI 名称和物种分类后，底层世界必须仍以完全相同的方式运行。

---

## 6. 基因与发育

### 6.1 基因组表示

首版基因组由四部分组成：

```text
Genome
├─ DevelopmentGenes 身体拓扑与局部生长规则
├─ MaterialGenes    连续材料、反应和运输参数
├─ RegulationGenes  基因表达条件与内部信号规则
└─ ControllerGenes  可变控制网络与结构端口连接
```

身体基因不直接存储最终模型，而存储生成规则：

- 从哪个父结构生长。
- 达到什么成熟度时出现。
- 生长方向、长度和宽度。
- 是否镜像或重复。
- 如何在有限预算中分配材料属性。
- 表面对哪些光、化学物质、温度和压力作出响应。
- 与其他区域建立哪些几何、物质和信号连接。
- 颜色和视觉标记。
- 受哪些内部状态抑制或促进。

### 6.2 发育过程

新生个体从携带基因组的最小遗传区域开始。随着能量、物质、局部环境和内部信号变化，发育规则增生、分叉、重复、连接或停止结构区域。

```text
遗传区域
   ↓ 局部生长规则
无语义材料区域逐渐增生与连接
   ↓ 结构和环境共同作用
在实际生存中表现出运动、感知、摄取或其他用途
```

首版不运行真实细胞分裂。发育执行器以事件方式添加或改变组织区域，并通过结构缩放、分叉、融合和逐步显现表达生长。基因只描述局部发育规则，不存储最终身体模型或器官清单。

### 6.3 突变类型

每次繁殖根据可遗传突变率发生：

- 数值扰动：长度、角度、材料属性、响应阈值和控制权重发生小变化。
- 基因复制：复制一个结构及其部分子结构。
- 基因删除：移除非核心结构。
- 连接改变：结构改挂到另一个合法父节点。
- 对称改变：新增、移除或改变镜像关系。
- 调控改变：改变结构出现的时间和条件。
- 材料改变：改变局部材料预算、反应特性和信号端口。
- 控制网络改变：权重变化、增加或删除神经节点或连接。

突变后必须通过安全验证：

- 无循环或非法身体连接。
- 核心结构存在。
- 结构数不超过限制。
- 所有数值处于安全边界。
- 无效基因可以出生后迅速死亡，但不能导致程序异常。

### 6.4 遗传稳定性

为了使进化具有连续性：

- 大多数突变应当很小。
- 结构突变相对少见。
- 基因复制后允许两个副本逐步分化。
- 不要求每个突变都有益。
- 突变概率本身可以在受限范围内遗传。

第一版采用无性繁殖，使后代谱系和结构变异更容易实现、调试和解释。

---

## 7. 生理与生态

### 7.1 能量与物质分离

生命至少需要两类守恒量：

- `Energy`：用于移动、维护、发育和繁殖。
- `Matter`：用于增加身体质量和制造后代。

光照只提供能量，不能凭空制造身体。矿物、有机物或猎物提供物质。

### 7.2 每个代谢周期

```text
光化学反应 / 表面扩散 / 接触捕获 / 催化分解
                      ↓
                获得能量和物质
                      ↓
局部维护 → 收缩与传导 → 修复 → 生长 → 遗传区域复制
                      ↓
               不足时损伤和死亡
```

死亡后，个体的大部分身体物质转化为所在区域的有机残骸。这样可形成生产者、消费者和分解者之间的物质循环。

### 7.3 生存成本

所有优势必须具有代价：

- 更大身体需要更多维护能量。
- 更多主动收缩材料增加瞬时能耗。
- 更厚防护增加质量、降低速度。
- 更多响应表面和更长信号网络增加持续能耗。
- 更高繁殖率降低单个后代的初始资源。
- 同一有限材料预算分配给更多属性，会降低每项属性可达到的峰值。

### 7.4 接触与捕食

首版不使用每个个体的完整刚体物理。宽阶段使用生物宏观包围圆或胶囊寻找潜在接触，窄阶段只检查真正靠近的结构段。

邻居通过空间哈希网格查询：

1. 根据位置找到当前空间桶。
2. 仅检查本桶和相邻桶。
3. 比较平面距离、深度差和交互方向。
4. 根据接触面的形状、相对速度、韧性、渗透性、催化物和浓度差结算形变、损伤与物质交换。

模拟核心不存在“攻击”或“捕食成功”按钮。一个结构刺穿另一个结构、分泌物使其分解并吸收释放物质后，观察系统才将这段关系描述为攻击或捕食。同样的结构也可能在另一环境下用于移动、防御或处理残骸。

### 7.5 死亡条件

- 能量耗尽且无法维持基础代谢。
- 健康降至零。
- 长期处于无法承受的温度、水深或干燥环境。
- 达到由基因影响的寿命上限。
- 不再含有可维持的遗传载体区域，或身体所有连通分量均失去维持能力。

---

## 8. 行为系统

### 8.1 原则

行为不由固定职业或预制 AI 决定，而由可遗传的小型控制网络产生。网络无需训练，直接随遗传变异并接受自然选择。

### 8.2 输入

控制器不能直接获得“最近猎物在哪里”或“对方是不是亲属”等上帝视角数据。只有身体上实际形成、暴露于环境并连接到信号网络的响应区域才能产生输入。

每个响应区域可以产生：

- 所在局部位置的光谱与光强响应。
- 所在局部位置的温度和化学物质响应。
- 接触压力、材料形变和局部损伤信号。
- 内部能量、物质浓度和运输压力。
- 由其他结构传来的内部信号。
- 可遗传的内部周期振荡。

方向感不作为现成输入提供，而由分布在身体不同位置的多个响应区域之间的信号差异产生。亲缘识别必须通过可演化的分泌物和化学响应形成，不能直接读取基因距离。

### 8.3 输出

控制器同样不直接输出“前进”“攻击”或“繁殖”等高层动作。输出只能连接到身体区域的底层可控属性：

- 改变某个区域的收缩强度。
- 改变局部渗透率、开合程度或附着强度。
- 释放某类化学物质或内部信号。
- 调节局部材料运输、修复和生长信号。
- 触发遗传载体的复制或结构连接的分离条件。

多个收缩区域按特定时序工作后，才可能涌现前进、转向、游动、搏动或缠绕。分泌、穿刺和吸收组合后，才可能涌现捕食。信号被另一生物利用后，才构成交流。

### 8.4 控制网络

首版使用小型循环信号网络：

- 输入和输出端口随身体区域及其材料属性产生，不使用固定动作列表。
- `0–12` 个可变内部节点。
- 权重、偏置、连接和节点均可突变。
- 保存少量上一时刻状态，从而产生简单记忆和周期行为。

必须限制网络规模和执行次数，防止“大脑越大就无条件占优”或计算量无限增长。每个内部节点具有明确能量成本。

---

## 9. 繁殖、种群与选择

### 9.1 遗传延续与身体分离

系统不预设生殖器官或单一繁殖动作。生命底层只有一条必要规则：基因组可以在消耗能量和物质后复制到新的遗传载体区域；当一个包含有效遗传载体的身体连通分量与原身体分离，并拥有最低维持资源时，它成为独立个体。

```text
复制遗传载体
      +
局部生长与资源转移
      +
结构自然断开或主动分离
      ↓
产生可独立维持的后代
```

由此可以在同一规则下逐渐形成：

- 近似均分的分裂。
- 从身体局部长出的出芽。
- 大量微小而低投入的传播体。
- 身体断裂后多个片段继续生存。
- 对后代投入大量资源的少量繁殖。

第一批共同祖先只携带一种最简单的可行分裂规则，之后的繁殖形态由发育、结构和调控共同进化。第一版仍只继承单一亲本基因组；两个个体之间的基因融合留到有性繁殖阶段。

### 9.2 密度控制

不使用直接的“系统强制人口上限”作为主要生态规则。数量应受到以下因素限制：

- 资源总量和恢复速度。
- 空间拥挤。
- 维护成本。
- 捕食和竞争。
- 后代资源投入。

系统仍保留硬性安全上限。达到上限时暂停新生，并在开发界面记录性能警告，不能静默删除现有个体。

### 9.3 选择来源

- 空间资源差异。
- 温度和水深差异。
- 季节变化。
- 竞争者和捕食者。
- 迁移成本。
- 身体结构的能耗权衡。

首版至少提供一个包含明显环境梯度的默认世界，确保分化拥有发生条件。

---

## 10. 谱系与自动分类

### 10.1 谱系记录

每个出生事件至少记录：

```text
个体 ID
亲代 ID
基因组 ID / 哈希
出生时间
死亡时间与原因
出生位置
所属物种
关键结构摘要
```

相同基因组只保存一份完整数据，个体引用基因组 ID，以降低存档体积。

### 10.2 物种划分

由于第一版是无性繁殖，不能使用交配隔离作为主要标准。采用组合距离：

```text
分类距离 =
    基因距离 × 权重
  + 身体结构距离 × 权重
  + 行为与生态位距离 × 权重
```

一个分支必须同时满足以下条件，才能被宣布为新物种：

- 与原物种中心距离超过阈值。
- 独立繁衍超过一定代数。
- 活跃种群超过最低数量。
- 不是短暂出现后立即消失的单个突变体。

物种分类属于观察工具，不反过来影响自然选择。

### 10.3 科、属、种层级

- 种：近期稳定繁殖的相似基因簇。
- 属：共享较近共同祖先的多个物种分支。
- 科：在身体基本结构上共享深层祖先的较大演化支系。

高层分类优先依据共同祖先和结构创新，而不是只按当前外观聚类，以避免趋同进化造成错误归类。

### 10.4 自动命名

名称使用确定性的音节生成器：

- 同一世界种子和谱系 ID 始终得到相同名字。
- 同属物种共享词根。
- 玩家可以为任何分类单元重命名。
- 系统保留原始自动名，避免改名破坏引用。

示例：

```text
维拉科 Velaridae
└─ 维拉属 Velara
   ├─ 浅滩维拉 Velara luma
   └─ 深水维拉 Velara nera
```

---

## 11. 玩家界面

### 11.1 世界视图

- 自由移动、缩放和旋转摄像机。
- 点击个体或物种群体进行观察。
- 显示出生、繁殖、捕食和死亡等可选标记。
- 支持隐藏界面，纯观察生态。

### 11.2 时间控制

```text
暂停 | 单步 | 1× | 10× | 100× | 1000× | 最大速度
```

倍率改变单位现实时间内执行的固定模拟步数量，不改变单步时长。这样同一个世界在 `1×` 和 `100×` 下应遵守相同规则，而不会因为加速出现不同的代谢或运动结果。

在高倍率下：

- 降低画面刷新和统计图采样频率。
- 每完成一批模拟步刷新一次 UI。
- 始终完整结算出生、死亡、突变、资源交换和谱系事件。
- 显示“目标倍率”和机器实际达到的倍率。
- “最大速度”可以关闭世界渲染，只保留进度和关键事件。

界面同时显示模拟日期、世界年龄、总世代数和当前物种的平均世代长度，避免“经过很多年”和“经过很多代”被混为一谈。

### 11.3 实验干预工具

玩家可以在世界运行中暂停或不暂停地进行干预。所有空间工具采用连续圆形笔刷，并可调整半径、强度和边缘衰减：

- 抬高或降低地形。
- 增加或移除水量。
- 加热或降温。
- 增减矿物、有机物与湿度。
- 调整局部光照或浑浊度。
- 投放初始生命、克隆选中个体或引入已保存基因型。
- 对局部区域施加短期灾害，用于压力实验。

每次干预作为带时间戳的历史事件保存，记录工具、位置、半径和参数。分类与统计界面应能区分自然变化和玩家干预后的变化。

### 11.4 个体检查器

显示：

- 年龄、能量、健康和繁殖状态。
- 身体区域的几何、材料属性及观察系统推断的可能用途。
- 各响应区域产生的局部信号与控制网络的区域激活输出。
- 父代、后代和所属物种。
- 与物种平均值的差异。
- 当前行为产生的主要能量收支。

### 11.5 物种页面

- 当前数量与历史数量。
- 分布地图。
- 平均身体结构。
- 主要食物和天敌。
- 平均基因与内部差异。
- 首次出现时间。
- 最近共同祖先。
- 分支、迁移与灭绝记录。

### 11.6 环境图层

至少支持：

- 高度
- 水深
- 温度
- 光照
- 矿物
- 有机残骸
- 生物密度
- 物种分布

### 11.7 演化时间线

自动记录但不过度打扰玩家：

- 新物种稳定形成。
- 物种灭绝。
- 种群增长或崩溃。
- 首次出现显著结构。
- 谱系迁移到新的高度、水深或环境带。
- 生态优势物种发生更替。

---

## 12. 美术与表现

### 12.1 风格

- 抽象、克制、低多边形。
- 通过形状、颜色、运动节奏表达生物功能。
- 避免过度写实，以免程序生成结构显得畸形或廉价。
- 世界整体像一座可以无限观察的生态模型箱。

### 12.2 程序化生物模型

所有生物由少量基础网格组成：

- 球体或椭球：表达紧凑的体积区域。
- 胶囊或管段：表达细长或分支区域。
- 圆锥或渐缩段：表达截面持续变化的区域。
- 平面或薄片：表达高表面积的薄层区域。
- 弧面块：表达覆盖在其他区域外侧的表层。

这些只是几何表达方式，不对应器官类别。基因决定尺寸、角度、连接、对称、重复、颜色和材料。动画由局部收缩信号和结构受力结果程序化驱动，不为每个物种单独绑定骨骼。

### 12.3 低模与细节层级预算

生物模型以“能读出轮廓、结构和运动”为最低面数原则，不追求光滑曲面。建议预算：

| 显示层级 | 使用场景 | 单个生物三角形目标 | 表现方式 |
|---|---|---:|---|
| LOD 0 | 选中或极近距离 | 平均不超过 600 | 显示完整区域结构与连接 |
| LOD 1 | 正常观察距离 | 平均不超过 180 | 低边数基础体组合 |
| LOD 2 | 远距离 | 不超过 48 | 合并轮廓或极简代理体 |
| LOD 3 | 极远或大群落 | 2–12 | 广告牌、点实例或密度云 |

补充约束：

- 默认球体、胶囊和管段使用低边数版本，禁止为圆滑增加不可见细分。
- 基础几何、材质和调色板全世界共享，不为每个个体创建独立 Mesh 或 Material。
- 身体变形优先修改实例变换或着色器参数，不每帧重新生成网格。
- 普通个体不使用独立骨骼、蒙皮、刚体或实时阴影。
- 只有选中个体和镜头近处少量结构获得轮廓、高亮或精细阴影。
- 同屏动态结构实例目标不超过 `20,000`，超过后主动合并到更低 LOD。
- 标准观察画面的可见三角形目标控制在 `1,000,000` 以内。
- 常规场景 Draw Call 目标不超过 `150`，主要依靠 MultiMesh 和共享材质批处理。

LOD 只改变画面，不改变生物身体数据、环境交互或进化结果。

### 12.4 深浅表现

- 浅水透明、明亮并能看到海底。
- 深水随深度变暗、偏蓝并增加雾化。
- 水下光照按深度和浑浊度衰减。
- 海底颜色受矿物、有机物和光照影响。
- 深水结构可通过高对比色或生物发光表现。

---

## 13. 技术架构

### 13.1 技术选型

- 引擎：Godot 4.x，C# 版本。
- 模拟核心：纯 C# 数据与系统，尽量不依赖 Godot 节点。
- 表现层：Godot 场景、材质、相机、音效和 UI。
- 测试：C# 单元测试加无画面集成测试。
- 首发目标：Windows 桌面。

### 13.2 基准硬件与“流畅”定义

首版性能验收机定义为：

- 桌面版 NVIDIA GeForce RTX 3060 级或更高显卡。
- 6 核 12 线程桌面 CPU，约 Ryzen 5 3600 / Core i5-10400 级或更高。
- 16 GB 系统内存。
- SSD。
- Windows 10/11，`1920 × 1080` 分辨率，低模标准画质。

“流畅运行”必须量化为：

- 标准世界、`5,000` 个存活生物、约 `1,000` 个可见生物时，正常观察平均不低于 `60 FPS`。
- 同一场景的 1% Low 不低于 `45 FPS`，不得周期性长时间卡顿。
- 正常时间倍率下模拟不积欠待执行步骤。
- 自动存档和物种统计不能造成超过 `100 ms` 的可感知停顿。
- GPU 显存占用目标低于 `4 GB`，整个游戏进程内存目标低于 `4 GB`。

RTX 3060 笔记本版本因功耗差异较大，不直接等同于桌面验收机；在进入公开测试前单独验证。

### 13.3 模块边界

```text
Simulation.Core
├─ World
├─ Environment
├─ Genetics
├─ Development
├─ Organisms
├─ Behavior
├─ Ecology
├─ Taxonomy
├─ History
└─ Persistence

Presentation.Godot
├─ TerrainRenderer
├─ WaterRenderer
├─ OrganismRenderer
├─ Camera
├─ Selection
├─ UI
└─ Audio
```

模拟核心禁止直接访问场景节点。表现层读取只读快照或变更列表，并把玩家命令提交给模拟线程。

### 13.4 更新顺序

每个固定模拟步：

1. 应用玩家命令。
2. 更新必要的环境区块。
3. 构建或刷新空间索引。
4. 采样感知输入。
5. 执行行为控制器。
6. 处理移动和环境限制。
7. 处理接触力、分泌、催化和物质交换。
8. 结算能量反应、损伤、生长、修复和结构分离。
9. 处理遗传载体复制、突变和新个体形成。
10. 处理死亡与物质回收。
11. 写入谱系和生态事件。
12. 生成表现层需要的状态快照。

同一步内的新生个体从下一步开始行动，避免更新顺序造成偏差。

### 13.5 连续时间与倍率实现

世界时间在概念上连续前进，程序内部使用足够小且固定的时间步进行数值积分。离散时间步是计算实现细节，不能表现成回合，也不能随倍率改变大小。

```text
固定模拟步长：SimDelta
时间倍率：TimeScale
每帧应执行步数：累积现实时间 × TimeScale ÷ SimDelta
```

实现要求：

- 正常模式按累积器执行固定步，渲染在前后两个模拟状态之间插值。
- 高倍率按批次执行大量相同固定步，而不是放大 `SimDelta`。
- 批次之间处理取消、暂停、存档和 UI 刷新，避免程序长时间无响应。
- 图表和历史记录可以降采样，但生态事件不能因倍率提高而跳过。
- 高倍率前自动建立临时恢复点，防止长时间演算因异常丢失。
- 世界没有局数终点；关闭程序时世界暂停，读档后从保存的准确模拟时刻继续。
- 第一版不计算程序关闭期间的离线演化，以避免硬件速度和离线时长改变实验可复现性。

### 13.6 数据布局

优先采用结构数组或紧凑数组：

```text
Positions[]
Energy[]
Health[]
GenomeIds[]
SpeciesIds[]
```

避免：

- 每个生物一个 Godot Node。
- 每个结构一个独立场景节点参与逻辑更新。
- 在高频循环中频繁分配对象。
- 使用通用刚体模拟所有个体。
- 每步遍历全世界寻找邻居。

### 13.7 并行策略

首版首先实现正确、可复现的单线程版本，再并行化明确热点。

适合并行的任务：

- 环境分区更新。
- 个体感知采样。
- 行为网络求值。
- 不产生冲突的代谢预计算。
- 渲染实例缓冲构建。

出生、死亡、资源争夺和伤害结算通过命令缓冲集中提交，避免多个线程直接修改同一对象。

### 13.8 渲染策略

- 地形按区块生成低模网格。
- 水面由少量网格和着色器表现。
- 生物结构按基础网格类型分组，使用 MultiMesh 批量渲染。
- 远处个体只显示整体轮廓或单个低模实例。
- 只有被选中的个体显示完整结构、调试标签和精细轮廓。
- 选择个体优先通过模拟空间查询，不为所有个体创建碰撞体。

首版只降低渲染细节，不因摄像机位置改变生态模拟规则，避免“玩家看向哪里，哪里的进化就更精确”。

---

## 14. 随机性、确定性与存档

### 14.1 随机流

所有随机过程必须来自显式随机流：

- 世界生成流。
- 突变流。
- 个体行为噪声流。
- 环境事件流。

禁止在模拟核心随意调用全局随机函数。世界种子、模拟步和事件序号应足以定位随机结果。

### 14.2 可复现目标

同一程序版本、同一平台、同一初始存档和同一命令序列，应得到相同的关键状态校验值。

跨平台逐位一致不是第一版硬性目标，但存档内容必须保持逻辑兼容。

### 14.3 存档内容

- 版本号和世界种子。
- 当前模拟时间、固定步序号和累计世界年龄。
- 连续环境场的参数、修改记录与底层采样缓存。
- 所有存活个体。
- 去重后的基因组注册表。
- 物种与谱系摘要。
- 玩家命名和标记。
- 关键历史事件。
- 随机流状态。

长时间历史采用摘要和关键节点，不永久保存每个普通个体的完整每帧状态。

### 14.4 存档体验

- 支持多个世界存档，每个世界具有独立名称、种子、创建时间和世界年龄。
- 支持随时手动保存。
- 按现实时间和模拟年份分别设置自动保存条件。
- 最大倍率运行期间定期建立滚动检查点。
- 写入采用临时文件完成后再替换正式存档，避免写入中断破坏唯一存档。
- 默认保留最近数个自动存档，手动存档不会被自动覆盖。
- 载入后恢复所有活体、环境、谱系、历史、玩家干预和随机状态。
- 存档版本不兼容时必须明确提示，禁止无提示丢弃基因或谱系数据。

---

## 15. 性能预算

以下数据均以第 13.2 节的 RTX 3060 级标准测试机为基准：

| 项目 | 首版硬性目标 | 扩展目标 |
|---|---:|---:|
| 连续世界范围 | 512×512 单位 | 1024×1024 单位 |
| 环境场隐藏采样 | 256×256 | 按字段自适应 |
| 存活个体 | 5,000 | 10,000 |
| 单个身体区域 | 常态 1–16，硬上限 32 | 复杂度预算制 |
| 可见个体 | 1,000 | 2,000 |
| 正常渲染平均值 | 60 FPS | 60 FPS 以上 |
| 1% Low | 45 FPS | 60 FPS |
| 可见三角形 | 不超过 1,000,000 | 不超过 500,000 |
| 常规 Draw Call | 不超过 150 | 不超过 100 |
| 显存 / 进程内存 | 4 GB / 4 GB以内 | 2 GB / 3 GB以内 |
| 基础模拟 | 10 tick/s | 20 tick/s |
| 无画面加速 | 10× 正常速度 | 50× 以上，硬件允许时继续提高 |

`50,000` 个体属于后续研究目标，不进入第一版范围。第一版先保证 `5,000` 个体、连续材料身体和观察工具同时运行时稳定流畅。

建议帧预算：

- GPU 渲染平均不超过 `12 ms`。
- Godot 主线程平均不超过 `10 ms`。
- 模拟线程不能在普通观察帧同步阻塞主线程超过 `2 ms`。
- 渲染状态传递采用双缓冲快照，不让主线程等待完整模拟步。

低模场景中 RTX 3060 通常不会是最先到达的瓶颈；个体感知、接触查询、信号网络和谱系记录更可能受 CPU 与内存访问限制。因此不能以降低面数代替模拟数据布局和空间索引优化。

性能退化顺序：

1. 降低非关键视觉动画频率。
2. 提前切换远处生物 LOD，并将群落改为密度表现。
3. 关闭普通个体实时阴影、轮廓和高成本水面效果。
4. 分批更新低变化环境字段。
5. 降低低活跃个体的信号求值频率，但不跳过能量、死亡和交互结算。
6. 进入无画面演算模式。

不得通过静默删除生物或改变选择规则来维持帧率。

---

## 16. 调试与研究工具

人工生命系统很难仅靠画面排错，以下工具属于核心开发功能：

- 单步运行一个模拟步。
- 固定世界种子。
- 查看个体全部感知输入和行为输出。
- 显示能量与物质收支。
- 显示空间索引和接触范围。
- 查看突变前后的基因差异。
- 强制克隆某个个体进行对照实验。
- 将两个基因型放入相同环境比较。
- 输出种群、出生率、死亡原因和平均复杂度曲线。
- 检查全局物质守恒误差。
- 无画面批量运行多个世界种子。

---

## 17. 测试策略

### 17.1 单元测试

- 基因序列化与反序列化一致。
- 每种突变都能生成合法或可安全淘汰的基因组。
- 发育结果在相同输入下稳定。
- 身体宏观属性缓存与完整重算一致。
- 空间查询不会遗漏相邻个体。
- 死亡物质正确回到环境。
- 名称生成在相同 ID 下稳定。
- 存档读回后关键状态一致。

### 17.2 模拟不变量

- ID 永不重复。
- 能量不能无来源增加。
- 除明确外部输入外，物质总量误差处于允许范围。
- 已死亡个体不会继续行动或繁殖。
- 结构和控制网络不会超过硬上限。
- 分类系统不会影响生物属性与适应度。

### 17.3 长时间测试

- 连续运行至少数千代不崩溃。
- 个体总数不会因数值异常无限爆炸。
- 基因和谱系存储不会无上限线性膨胀。
- 同一测试种子重复运行得到相同校验值。
- 在具有环境梯度的基准地图中，可以出现持续多个世代的谱系差异。

### 17.4 性能回归测试

仓库必须保存一个固定的标准性能存档，其中包含 `512 × 512` 世界、`5,000` 个存活生物、约 `1,000` 个镜头可见生物和具有代表性的复杂身体。

测试规则：

- 使用导出的 Release 版本，不以编辑器帧率作为验收依据。
- 固定分辨率、摄像机路径、世界种子和观察时长。
- 预热后采集至少 `120` 秒数据。
- 分别记录平均 FPS、1% Low、GPU 时间、主线程时间、模拟线程时间、内存和显存。
- 另行运行关闭渲染的最大速度测试，记录每秒模拟步数和每模拟年的现实耗时。
- 每次新增底层材料属性、感知通道或身体交互规则后运行对比。
- 若平均性能下降超过 `5%`，必须在合并前解释原因；超过预算则不能进入首版主分支。

---

## 18. 开发阶段

### 阶段 0：无画面生命循环

目标：证明能量、物质、繁殖和死亡闭环成立。

- 连续坐标和连续环境采样接口，底层暂用插值采样阵列。
- 圆点生物。
- 固定身体参数。
- 资源摄取、维护、繁殖和死亡。
- 固定种子与统计输出。

完成标准：系统可以在没有渲染的情况下稳定运行，且不存在明显资源凭空产生或数量无限爆炸。

### 阶段 1：遗传与发育

目标：身体由基因生成，变化能够遗传。

- 身体结构图。
- 发育阶段。
- 连续材料属性和底层反应规律。
- 数值和结构突变。
- 身体宏观属性缓存。

完成标准：同一基因在相同环境中产生一致身体；后代与亲代相近但偶尔出现可见结构差异；任何结构的用途都不是由器官类型字段直接赋予。

### 阶段 2：行为与生态位

目标：环境压力能够选择不同策略。

- 局部环境响应和内部信号系统。
- 小型循环控制网络。
- 区域收缩、渗透、分泌、生长和分离等底层输出。
- 空间哈希与接触结算。
- 至少一个陆地—浅水环境梯度测试场景。

完成标准：不同谱系可在不同区域形成稳定数量差异，行为差异能追溯到遗传参数。

### 阶段 3：低模世界表现

目标：将数据模拟变成可观察的游戏体验。

- 高度图地形与水体。
- 程序化低模生物。
- MultiMesh 批量渲染。
- 摄像机、选择和检查器。
- 连续笔刷干预、时间倍率控制与热力图。

完成标准：玩家可以从世界视图选择任意个体，理解其身体、环境和当前行为；在标准性能存档中达到既定 RTX 3060 帧率预算。

### 阶段 4：物种与自然史

目标：让长期变化可以被发现和讲述。

- 谱系数据库。
- 自动物种聚类。
- 科、属、种树。
- 自动命名。
- 演化事件时间线。
- 物种分布与数量曲线。

完成标准：玩家能够从现存物种回溯祖先，并找到一次分化或灭绝的原因线索。

### 阶段 5：首版打磨

目标：形成稳定可保存、可重复实验的首个公开版本。

- 手动存档、自动存档、滚动检查点和读档。
- 多档时间倍率与无画面最大速度演算。
- 性能分析与热点优化。
- 教学引导和默认世界。
- 崩溃保护与版本迁移。

---

## 19. 第一版验收标准

必须同时满足：

1. 玩家可以创建世界并观察至少 `5,000` 个个体稳定运行。
2. 模拟核心不存在预制物种或器官类型；身体由无语义材料区域、发育规则和连接拓扑生成。
3. 关闭 UI 的器官推断和自动命名后，模拟校验结果完全不变，证明语义标签不参与生存结算。
4. 至少两个测试谱系能通过材料、几何、连接或控制时序的变化，形成不同的移动或营养获取方式。
5. 后代能继承结构和行为，并出现大小不同的突变。
6. 世界中能形成生产、摄取和分解的基础物质循环。
7. 至少一个标准测试种子会在合理时间内形成两个以上持续存在的分化谱系。
8. 玩家能查看个体、物种、共同祖先和关键历史事件。
9. 生物移动、环境采样、水深变化和玩家笔刷均无可感知的格子边界。
10. 存档读回后世界从准确时间点继续演化，并保留全部活体、谱系和干预历史。
11. 改变时间倍率不会改变固定模拟步长，也不会跳过生命事件。
12. 最大速度演算可以在不渲染的情况下明显快于正常观察速度。
13. 长时间运行不会因基因损坏、谱系膨胀或人口爆炸而崩溃。
14. RTX 3060 级标准测试机在 1080p、5,000 个存活生物和约 1,000 个可见生物时，平均达到 60 FPS 且 1% Low 不低于 45 FPS。

以下内容出现属于额外成功，不作为首版硬性要求：

- 捕食行为自然稳定出现。
- 多物种长期共存。
- 信号被其他个体利用。
- 身体出现开发者未明确设计的功能组合。

---

## 20. 主要风险与对策

### 风险一：进化停留在简单高效形态

对策：引入空间异质性、季节、迁移成本、多种资源和明确功能权衡，不直接奖励复杂度。

### 风险二：系统出现唯一优势策略并占满世界

对策：确保资源具有局部性；引入密度竞争、连续环境梯度和随种群变化的生物压力。

### 风险三：突变大多产生不可用身体

对策：让大多数突变为小幅参数变化；结构变异低频发生；使用模块复制和合法性检查。

### 风险四：画面看似变化，实际只是随机参数

对策：提供谱系、基因差异、能量收支和环境数据，让玩家能建立因果解释。

### 风险五：性能优化过早拖慢玩法验证

对策：先完成无画面正确实现并建立基准测试，只对测量出的热点进行数据化和并行优化。

### 风险六：镜头位置影响模拟结果

对策：首版模拟精度不依赖摄像机；只对渲染使用距离细节层级。

### 风险七：连续参数退化成“换名字的预制器官”

如果某个单一参数直接等于视力、攻击力或消化效率，系统虽然没有器官枚举，本质上仍然写死了用途。

对策：能力必须至少由材料属性、空间位置、几何形态、结构连接和控制时序中的多个因素共同产生；所有语义名称只能由观察层事后推断，禁止进入适应度或交互结算。

---

## 21. 当前产品决策

已由项目方向确认或作为首版技术基线：

| 项目 | 方案 | 状态 |
|---|---|---|
| 游戏名称 | 《原生纪》 | 已确认 |
| 英文名称 | 待定 | 待确认 |
| 引擎 | Godot 4.x C# | 技术基线 |
| 首发平台 | Windows 桌面 | 技术基线 |
| 视觉 | 抽象低模 3D | 暂定 |
| 标准世界规模 | 512×512 连续单位，5,000 个体为首版验收负载 | 技术基线 |
| 性能基线 | 桌面 RTX 3060 级显卡，1080p 平均 60 FPS | 已确认 |
| 图形取舍 | 最低必要面数，生态密度与时间加速优先 | 已确认 |
| 空间表现 | 连续坐标、连续环境场和连续水深 | 已确认 |
| 底层数值方法 | 隐藏采样阵列、连续插值和解析函数混合 | 技术基线 |
| 身体部位 | 无预制器官，由无语义材料区域及其连接涌现 | 已确认 |
| 功能判定 | 底层只结算物理与反应，器官名称由观察层事后推断 | 已确认 |
| 游戏模式 | 单机实验箱，观察为主 | 已确认 |
| 玩家干预 | 允许运行中自由干预，并记录进世界历史 | 已确认 |
| 世界时间 | 无关卡终点、连续时间线、倍率可调 | 已确认 |
| 高速演化 | 支持关闭渲染后快速迭代大量年份与世代 | 已确认 |
| 存档 | 手动存档、自动存档与滚动检查点 | 已确认 |
| 初始生命 | 一个或少数共同祖先 | 暂定 |
| 繁殖 | 首版无性繁殖 | 待确认 |
| 控制系统 | 可遗传的小型循环神经网络 | 技术基线 |
| 世界边界 | 有限岛屿和深海边界 | 待确认 |
| 胜负条件 | 无，观察和发现驱动 | 已确认 |
| 模拟核心 | 独立于 Godot 场景树的纯 C# 数据系统 | 技术基线 |

---

## 22. 尚待确认的问题

以下问题不会阻止阶段 0 的技术原型：

1. “连续世界”是否指有限范围内坐标与环境连续，还是还要求地图无限生成、没有空间边界？本文当前按前者设计。
2. 美术视角更接近宏观星球生态、桌面生态箱，还是显微镜下的抽象生命？
3. 是否接受首版只做无性繁殖，并将有性繁殖留到核心闭环稳定之后？
4. 玩家干预是否包括直接修改或培育基因，还是只允许改变环境与投放已有生命？

---

## 23. 下一份技术文档

方向确认后，应继续产出《技术原型规格 v0.1》，具体定义：

- 仓库与 Godot 工程目录结构。
- 世界和生物数据的准确 C# 类型。
- 固定步长调度器。
- 随机数与确定性规则。
- 基因编码和突变操作的二进制格式。
- 无语义材料属性、成本约束和底层作用方程。
- 身体区域拓扑、发育指令和派生功能测试方法。
- 阶段 0 的测试地图与参数。
- 基准测试程序和首批单元测试。
